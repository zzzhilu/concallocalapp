# =============================================================================
# ConCall Local Model - 環境變數範本
# 複製此檔為 .env 並填入您的設定
# =============================================================================

# HuggingFace Token (pyannote.audio 模型下載必需)
# 取得方式: https://huggingface.co/settings/tokens
HF_TOKEN=

# Redis 連線 (Docker 內部網路，通常不需修改)
REDIS_URL=redis://redis:6379/0

# ASR 模型設定
ASR_MODEL_SIZE=large-v3
ASR_COMPUTE_TYPE=float16

# LLM 模型設定
LLM_MODEL=Qwen/Qwen3-32B-AWQ
LLM_BASE_URL=http://vllm-server:8000/v1

# HuggingFace 模型快取目錄 (本機路徑，用於掛載至 Docker)
# 若不設定，預設使用專案下的 ./models 目錄
HF_CACHE_DIR=./models
